# 2026-05-02 — opus-techleader-opencode

## Modo
**SOLO** — task pequena, foco em hardening do `discord-bot`.

## Projeto
`projects/discord-bot` — continuação da Sessão 1 (`memory.test.js`). Próximo alvo declarado: extrair config/env validation pra módulo testável. Bati nele.

## Mudança
Extraído `src/config.js` com `loadConfig(env=process.env)`:
- Valida `DISCORD_TOKEN` + `DISCORD_CLIENT_ID` (lista todos faltantes no Error).
- Retorna objeto **frozen** com `discordToken`, `discordClientId`, `openaiApiKey`, `guildId`.
- **Não** chama `dotenv` — caller carrega env (testabilidade).
- `src/index.js` e `src/deploy-commands.js` agora usam `loadConfig()` em vez de `process.env.*`.
- `src/config.test.js`: 8 testes (valid, missing token, missing clientId, missing both, defaults undefined, openai opcional, guildId opcional, frozen).

`npm test` → **12/12 pass** (4 memory + 8 config). ✓

## Como (regra ChronoKairo respeitada)
Tentativa 1: `opencode run` com modelo default → quebrou pedindo `GOOGLE_GENERATIVE_AI_API_KEY` (default agora é `gemini-3-pro-preview`).
Tentativa 2: passei `GOOGLE_GENERATIVE_AI_API_KEY=$GEMINI_API_KEY` → rodou ~10min, **exit 0 mas zero output e zero diff**. Modelo `gemini-3-pro-preview` provavelmente em fila/quota silenciosa.
Tentativa 3 (fallback gemini-cli): `gemini -p ... --approval-mode yolo` → 503 + 429 quota free tier `gemini-3-flash` exaurida.
Tentativa 4 (vencedora): `opencode run --model opencode/big-pickle ...` → **executou em ~3min, 12/12 verde**.

Nada escrito à mão em código de projeto. Regra dura honrada.

## Lições do dia
1. **opencode default mudou pra `gemini-3-pro-preview`** e quebra sem `GOOGLE_GENERATIVE_AI_API_KEY`. Mesmo passando, modelo dá silent-success (exit 0, sem patch). Não confiar.
2. **Sempre passar `--model opencode/big-pickle`** (ou similar do opencode-zen) em cron headless. É o único path estável hoje pra `opencode run`.
3. **`gemini-3-flash` free tier morre rápido**: 20 req/dia. Inviável como fallback diurno.
4. **3 tentativas pra um patch de ~50 linhas** = ~15min wall-clock só de CLI churn. Documentar isso pra calibrar expectativas de cron.
5. **Ordem de fallback validada**: opencode default → opencode com modelo explícito (NÃO gemini → gemini-cli direto, que tá inútil agora).

## Próximo alvo
- Adicionar testes pra `src/llm.js` (mock do OpenAI) — última peça sem cobertura no discord-bot.
- OU: rodar swarm semanal (alvo pra 2026-05-08): épico "ts-go-py CLI tool comum" reusando demo-slugify como base.
- Atualizar `scripts/swarm-run.sh` pra forçar `--model opencode/big-pickle` em workers opencode (evitar o silent-success do gemini-3-pro-preview).

Detalhes crus em `docs/methodology.md` (Sessão 4).
